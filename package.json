{
  "name": "todo-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev:web:express-ts": "npm run dev --workspace @todo-app/web-express-ts --if-present",
    "build:lib:interfaces": "npm run build --workspace @todo-app/lib-interfaces --if-present",
    "build:lib:repositories": "npm run build --workspace @todo-app/lib-repositories --if-present",
    "build:lib:services": "npm run build --workspace @todo-app/lib-services --if-present",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "workspaces": [
    "web/express-ts",
    "lib/interfaces",
    "lib/repositories",
    "lib/services"
  ],
  "devDependencies": {
    "@types/mongodb": "^4.0.6",
    "@types/node": "^22.9.3",
    "nodemon": "^3.1.7",
    "ts-node": "^10.9.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "mongodb": "^6.11.0"
  }
}
